# Storage v2 dependency note

The Storage v2 portal sidecar adds no third-party dependency. It uses the existing dependency-free Python portal, SQLite access already present in the project, and standard-library APIs only.
