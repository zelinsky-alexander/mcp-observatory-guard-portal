# Storage v2 sidecar branch status

This branch is for local-only side-by-side validation. The installer binds a separate portal to `127.0.0.1:8081` and does not alter the existing public portal service, Nginx, Cloudflare, or production timers.
