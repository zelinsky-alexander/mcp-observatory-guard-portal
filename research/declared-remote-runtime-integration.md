# Declared remote runtime portal integration

The portal contribution is read-only. It consumes already-published remote runtime schedule state and adds coverage metrics for declared, eligible, completed, blocked, inconclusive, failed, comparable and drifted remote observations. It exposes no public action that starts a remote probe.
