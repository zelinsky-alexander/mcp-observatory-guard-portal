# Install as /etc/mcp-portal/public-readonly.env, owned by root:root, mode 0644.
# Do not add feature flags, job paths, evidence paths, executable paths, or secrets.
MCP_PORTAL_DATABASE=/srv/mcp-portal-public/catalog.sqlite
MCP_PORTAL_PAGE_SIZE=50
